# .soia/config.yaml — Metadatos locales del proyecto
# Generado por: soia init --tool opencode
# NO editar manualmente — usar `soia fallback` o `soia models` para cambios

tool: opencode
initialized_at: "{{INITIALIZED_AT}}"
templates_source: "{{HOME}}/.config/soia/templates/opencode"