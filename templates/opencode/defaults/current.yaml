# .swarm/current.yaml — Tracker del change activo
# Generado por: swarm init
# Actualizado por: swarm-orchestrator en cada fase
# NO editar manualmente — usar /swarm-propose y /swarm-archive

active_change: null